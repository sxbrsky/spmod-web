function getData()
{
    /*
                    var r = new XMLHttpRequest();
                r.open('GET', 'http://localhost:8080/build/', true);
                r.onreadystatechange = function () {
                    if (r.readyState != 4 || r.status != 200) return;
                    var data = JSON.parse(r.responseText);
                    document.getElementById('result').innerHTML = "<h1>"+data[1].filename+"</h1>"
                }
        r.send();</script>
        */
}